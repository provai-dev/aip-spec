# Draft Snapshots

This directory stores append-only snapshots of minted Internet-Draft revisions.

Each revision uses the full draft name as its directory, for example:

- `draft-singla-agent-identity-protocol-00`
- `draft-singla-agent-identity-protocol-01`

Each snapshot preserves exact source, schemas, examples, generated outputs, and metadata for that revision.
